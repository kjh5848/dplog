import { RealtimeRankingDashboard } from '@/widgets/ranking';

/**
 * 실시간 순위 조회 페이지
 *
 * 대시보드 사이드바 > "실시간 조회" 메뉴에서 접근합니다.
 */
export default function RealtimeRankingPage() {
  return <RealtimeRankingDashboard />;
}

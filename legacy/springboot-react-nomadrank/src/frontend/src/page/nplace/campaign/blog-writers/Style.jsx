export default function NplaceCampaignBlogWritersStyle() {

  /** @type {React.CSSProperties} */
  const searchResultDivider = {
    marginTop: "30px",
    marginBottom: "30px"
  };

  /** @type {React.CSSProperties} */
  const cursorPointer = {
    cursor: "pointer"
  };

  /** @type {React.CSSProperties} */
  const blogCard = {
    padding: "0",
  };

  return {
    searchResultDivider,
    cursorPointer,
    blogCard
  };

}
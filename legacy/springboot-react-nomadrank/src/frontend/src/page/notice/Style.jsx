export default function NoticeListStyle() {

  /** @type {React.CSSProperties} */
  const searchResultDivider = {
    marginTop: "30px",
    marginBottom: "30px"
  };

  /** @type {React.CSSProperties} */
  const cursorPointer = {
    cursor: "pointer"
  };

  return {
    searchResultDivider,
    cursorPointer
  };

}